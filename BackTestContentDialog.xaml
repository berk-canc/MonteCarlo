<ContentDialog
    x:Class="MonteCarloPro.BackTestContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonteCarloPro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" KeyDown="ContentDialog_KeyDown">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="labelTitle"        Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="2" Margin="8,4"     VerticalAlignment="Bottom"    Text="Backtest portfolio" FontSize="14"  FontWeight="Bold"                                                                               />
        <TextBox   x:Name="textBoxStartYear"  Grid.Row="1"  Grid.Column="0" Grid.ColumnSpan="2" Margin="8,4"     Header="Starting year"        ToolTipService.ToolTip="Starting year"   MinHeight="48" Text="1980" InputScope="DateYear" MaxLength="4"  FontSize="12"                   />
        <TextBox   x:Name="textBoxEndYear"    Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="2" Margin="8,4"     Header="Ending year"          ToolTipService.ToolTip="Ending year"     MinHeight="48" Text="2015" InputScope="DateYear" MaxLength="4"  FontSize="12" IsReadOnly="True" />
        <CheckBox  x:Name="checkBoxInflation" Grid.Row="3"  Grid.Column="0" Grid.ColumnSpan="2" Margin="8,4"     Content="Use actual inflation data for this time period"               ToolTipService.ToolTip="Use US CPI data for this time period"   FontSize="12"                   />
        <Button    x:Name="buttonOk"          Grid.Row="4"  Grid.Column="0" Content="Start"     Margin="8,4,8,0" HorizontalAlignment="Stretch" Click="ButtonOk_Click"    />
        <Button    x:Name="buttonCancel"      Grid.Row="4"  Grid.Column="1" Content="Cancel"    Margin="8,4,8,0" HorizontalAlignment="Stretch" Click="ButtonCancel_Click"/>
    </Grid>
</ContentDialog>